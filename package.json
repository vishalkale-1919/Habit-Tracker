{
  "name": "neural-habit-tracker",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install --prefix client && npm run build --prefix client",
    "install": "npm install --prefix client"
  }
}